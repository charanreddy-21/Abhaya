import { SafePlacesView } from '@/components/domain/safe-places';

export const metadata = { title: 'Safe places — Abhaya' };

export default function SafePlacesPage() {
  return <SafePlacesView />;
}
