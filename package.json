{
  "name": "abhaya",
  "version": "0.1.0",
  "private": true,
  "description": "Hackathon prototype for a PWA-first public safety platform for India.",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "node scripts/run-web.mjs dev",
    "build:web": "node scripts/run-web.mjs build",
    "start:web": "node scripts/run-web.mjs start",
    "dev:server": "python -m uvicorn apps.server.main:app --reload --host 0.0.0.0 --port 8000"
  }
}
